# examples/ — Standalone example applications
#
# Each subdirectory is a self-contained example with its own CMakeLists.txt.
# Examples are only built when explicitly requested (e.g. BAREMETAL_DEMO=ON).

add_subdirectory(bare-metal)